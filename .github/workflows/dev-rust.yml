name: dev-rust

on:
  push:
    branches: [main]
    paths: ['toolchain/rust/**']
  pull_request:
    branches: [main]
    paths: ['toolchain/rust/**']

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        if: github.ref == 'refs/heads/main'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: toolchain/rust
          load: true
          tags: |
            ghcr.io/sqltgen/dev-rust:latest
            ghcr.io/sqltgen/dev-rust:${{ github.sha }}
          cache-from: type=gha,scope=dev-rust
          cache-to: type=gha,scope=dev-rust,mode=max
      - run: docker run --rm ghcr.io/sqltgen/dev-rust:latest sh -c "rustc --version && cargo --version"
      - name: Push
        if: github.ref == 'refs/heads/main'
        run: |
          docker push ghcr.io/sqltgen/dev-rust:latest
          docker push ghcr.io/sqltgen/dev-rust:${{ github.sha }}
